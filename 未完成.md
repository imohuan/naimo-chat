第一次创建 canvas 对话，你的编辑框无法打开
历史记录保存错误（最重要的）
关闭编辑器之后 请在一个地方显示 可以重新打开编辑器

diff 单独接受了一个就直接退出了
diff 消失之后，我希望你可以给代码快和消息快添加一个应用 diff 的功能
diff 接受按钮位置向右移动一些

```
------- SEARCH
            document.body.addEventListener('click', function(e) {
                createFirework(e.clientX, e.clientY);
            });

            function createFirework(x, y) {
=======
            document.addEventListener('mousemove', function(e) {
                createFirework(e.clientX, e.clientY);
            });

            function createFirework(x, y) {
>>>>>>> REPLACE
```

```
11<<<<< SEARCH
            document.body.addEventListener('click', function(e) {
                createFirework(e.clientX, e.clientY);
            });

            function createFirework(x, y) {
=======
            document.addEventListener('mousemove', function(e) {
                createFirework(e.clientX, e.clientY);
            });

            function createFirework(x, y) {
>>>>>>> REPLACE
```

添加一个临时显示编辑器的属性，当第一次消息监听到第一个流写入的时候判断这个临时属性是否已经开启，如果没有开启的话，则开启 这个属性切换对话的时候就把它给关闭，这样的话 即使是没有历史记录 也可以打开编辑器, 这种情况下如果切换对话（这个编辑器就会消失）

结束流写入的时候，这里需要进行一个判断不是 diff 的情况下，执行一次保存

在 diff 的情况下，接受了全部退出普通模式的情况同样执行一次保存

上面两个情况保存之后 会将版本的历史记录进行一个保存

在判断 diff 模式的情况下，添加两种格式符号的判断，在真正调用 diff 的情况下 统一将其设置为标准的符号
